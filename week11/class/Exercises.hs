module Exercises where

{- Exercise:
Ask the user for two numbers and print their sum.
Make sure to check that it works using the terminal. -}

addTwo :: IO ()
addTwo = error "unimplemented"

{- Exercise:
Given an integer [n], ask the user to repeatedly
guess a number until they get [n]. Tell them if their
guess is higher or lower than expected. If they get
the right answer, tell them so and end the game. -}

guessGame :: Int -> IO ()
guessGame = error "unimplemented"

{- Exercise:
Have a classmate test out your game in the terminal.
See if you can find any bugs in the one you test!
(For this exercise, it's OK to crash on non-integer
inputs.) -}